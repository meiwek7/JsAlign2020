SVG.on(document, 'DOMContentLoaded', function(){

    // let draw = SVG().addTo('#test').size(400, 400);

    // let rect = draw.rect(300, 300).fill('orange');

    // let line = draw.line([[0,150],[300,150]]).stroke({width:2, color:'red'});

    // let line2 = draw.line([[150,300],[150,-300]]).stroke({width:2, color:'red'});

    // let polygon = draw.polygon('150,150 300, 0').fill('orange').stroke({width:1});

    // rect.radius(10, 20);

    // let triggle = draw.polyline([[0,150],[150,0],[300,150]]).fill('blueviolet');


    // let circle = draw.circle(150,150).fill('pink');

    // circle.animate().fill('black');

    // let draw = SVG().addTo('lin')
});